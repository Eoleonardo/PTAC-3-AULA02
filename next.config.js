/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            "ead.ifms.edu.br",
            "www.ifms.edu.br",
            "https://moodle.org"
        ]
    }
}

module.exports = nextConfig
